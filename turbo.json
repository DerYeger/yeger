{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "typecheck": {
      "dependsOn": ["^build"],
      "inputs": [
        "vite.config.ts",
        "tsconfig.json",
        "index.html",
        "src/**",
        "test/**"
      ],
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["vite.config.ts", "tsconfig.json", "index.html", "src/**"],
      "outputs": ["dist/**"]
    },
    "preview": {
      "dependsOn": ["build"],
      "cache": false
    },
    "test": {
      "inputs": ["vite.config.ts", "src/**", "test/**"],
      "outputs": ["coverage/**"]
    },
    "dev": {
      "outputs": [],
      "cache": false
    },
    "lint": {
      "inputs": ["*"],
      "outputs": []
    }
  }
}
