{
  "name": "demo",
  "private": true,
  "sideEffects": true,
  "scripts": {
    "dev:demo": "vite --config vite.config.ts",
    "typecheck": "vue-tsc --noEmit main.ts",
    "prebuild": "rimraf ./dist",
    "build": "vite --config vite.config.ts build",
    "preview": "vite --config vite.config.ts preview"
  },
  "dependencies": {
    "@yeger/vue-masonry-wall": "workspace:*"
  }
}
