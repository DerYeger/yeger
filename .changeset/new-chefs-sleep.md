---
'vue-fast-mount': minor
---

refactor plugin to run on compiled SFCs for better coverage support

`wrapper.html()` should now match a regular `shallowMount` usage.
v8 coverage should now be more accurate.
